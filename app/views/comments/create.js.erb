<% unless @comment.errors.present? %>
  $('.comments .messages').append('<%= j render 'recipes/comment', comment: @comment %>')
  $('.no-comments').hide()
  $('#comment_text').val('')
  $('.comment-errors').html('')
<% else %>
  $('.comment-errors').html('<%= j error_messages(@comment) %>')
<% end %>
