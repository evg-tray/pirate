= "#{recipe.average_rating}/5 "
sup title=t('recipes.rating.votes_count')
  = recipe.count_rating
|&nbsp
.user-rating.inline data-score="#{user_score}" data-readonly="#{read_only}" id="#{recipe.id}" data-cancel="#{!read_only}"
|&nbsp
