.panel.panel-default
  table.table.table-striped.v-center
    thead
      th
        = t('activerecord.attributes.recipe.name')
      th.fit
        = t('activerecord.attributes.recipe.author')
      th.fit
        = t('activerecord.attributes.recipe.created_at')
      th.fit
        = t('recipes.table.table.rating')
      th.fit
    tbody
      - recipes.each do |rec|
        = render 'recipes/favorite_table/item', recipe: rec
